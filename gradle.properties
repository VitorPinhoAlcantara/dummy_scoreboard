org.gradle.jvmargs=-Xmx1G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# Environment Properties (kept in sync with training_dummy's 1.21.1 branch - same target platform)
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17
minecraft_version=1.21.1
minecraft_version_range=[1.21.1]
neo_version=21.1.244
# The loader version range can only use the major version of FML as bounds
loader_version_range=[1,)

# The Training Dummy mod this addon extends. Pulled from its local file-repo (see build.gradle) -
# run `./gradlew publish` in the training_dummy-1.21.1 project after any change there before
# building this addon, so the local repo has a matching jar.
trainingdummy_version=1.5
# Dev-test only, to verify curio-equipped snapshots (see build.gradle comment).
curios_version=9.5.1+1.21.1
# Dev-test only, same versions as training_dummy-1.21.1 - installed alongside it in runClient so
# the scoreboard dummy can be checked against the same mods the base dummy is tested with.
artifacts_version=13.2.1
jei_version=Tbomhybz
jade_version=eYz2YBGT

## Mod Properties

mod_id=dummyscoreboard
mod_name=Dummy Scoreboard
mod_license=All Rights Reserved
mod_version=1.2
mod_group_id=com.dummyscoreboard
