org.gradle.jvmargs=-Xmx1G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

# Environment Properties (kept in sync with training_dummy - same target platform)
minecraft_version=26.1.2
minecraft_version_range=[26.1.2]
neo_version=26.1.2.87

# The Training Dummy mod this addon extends. Pulled from its local file-repo (see build.gradle) -
# run `./gradlew publish` in the training_dummy project after any change there before building
# this addon, so the local repo has a matching jar.
trainingdummy_version=1.4
# Dev-test only, to verify curio-equipped snapshots (see build.gradle comment).
curios_version=15.0.0+26.1.2
# Dev-test only, same versions as training_dummy - installed alongside it in runClient so the
# scoreboard dummy can be checked against the same mods the base dummy is tested with.
artifacts_version=xrizTBU2
jei_version=OhedJ3Iu
jade_version=fnbQ0Mb3

## Mod Properties

mod_id=dummyscoreboard
mod_name=Dummy Scoreboard
mod_license=All Rights Reserved
mod_version=1.0
mod_group_id=com.dummyscoreboard
